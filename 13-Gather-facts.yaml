- name: gather facts
  hosts: local
  connection: local 
  # ansible collects the info of target server, before it executes any other facts
  tasks:
  - name: facts 
    ansible.builtin.ping:
      msg: "{{ ansible_facts }}"
