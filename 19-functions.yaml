 name: functions
  hosts: local
  connection: local
  vars:
    technologies: DevOps, Linux, Shell, Ansible
  tasks:
  - name: dafault variable
    ansible.builtin.debug:
      msg: "Hello, {{ name | default('Ramesh')}}"
  tasks:
  - name: convert string to list
    ansible.builtin.debug:
      msg: "Technologies are: {{ technologies | split(',')}}"